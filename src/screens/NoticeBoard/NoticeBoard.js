import React from "react";
import { View, Text } from "react-native";

const NoticeBoard = () =>{
    return (
        <View style={{ flex:1,justifyContent:"center",alignItems:"center"}}><Text>I am your NoticeBoard screen</Text></View>
    );
}

export default NoticeBoard;